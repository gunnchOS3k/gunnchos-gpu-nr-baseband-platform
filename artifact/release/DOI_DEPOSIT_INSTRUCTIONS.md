# DOI deposit instructions

1. Create a Zenodo deposit from the GitHub release tag.
2. Replace `DOI_PENDING` in `CITATION.cff` and `codemeta.json`.
3. Do not mint a DOI until CPU smoke + declared GPU/NIC statuses are honest.
