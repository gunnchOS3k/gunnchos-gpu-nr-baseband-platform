# Start here (supervisor) — GPU NR baseband

Optional **research extension**, not a dissertation paper.

1. Read `docs/CLAIM_BOUNDARIES.md` (six classes).  
2. Skim UML: `docs/uml/current/index.md`.  
3. Reproduce CPU: `REPRODUCIBILITY.md` → `make supervisor-cpu-gate`.  
4. GPU: expect `results/blocked_gpu/BLOCKED_GPU.json` unless an NVIDIA lab runner exists.  
5. Visibility: `docs/packets/REPOSITORY_VISIBILITY_PACKET.md` if this clone was private.

No 3GPP conformance, no carrier-grade claim, no Oulu appointment.
