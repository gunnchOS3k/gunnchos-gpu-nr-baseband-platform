# Gate 6 — NIC / PTP protocol

1. Document NIC model, driver, firmware in lab-instrument manifest.
2. Measure hardware timestamps / PTP offset with lab tools (pending).
3. Fronthaul emulator timestamps are software clocks unless lab PTP is attached.
4. Without NIC/PTP hardware: `PENDING_LAB` / `BLOCKED_HARDWARE`.
